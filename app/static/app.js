// Highlight wikilinks that point to non-existent pages (optional, graceful)
// and handle any other lightweight client-side behaviour.

document.addEventListener('DOMContentLoaded', () => {
  // Nothing required for basic browsing — all rendering is server-side.
  // The ask-page SSE logic lives inline in query.html for co-location.
});
